const defaultState = {
  items: [],
  isFetching: false
}

export default function apiReducer(state = defaultState, action) {
  switch (action.type) {
    default:
      return state
  }
}
